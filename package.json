{
  "name": "sileo-vue-root",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter docs dev",
    "build": "pnpm --filter sileo-vue build",
    "build:all": "pnpm -r build",
    "type-check": "pnpm --filter sileo-vue type-check"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "@parcel/watcher",
      "esbuild"
    ]
  }
}
